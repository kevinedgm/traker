<script setup>defineProps({eyebrow:{default:'Retomar'},headline:{default:'Qué bueno que volviste.'},body:{default:'No necesitas recuperar los días anteriores. Empezamos desde hoy.'},actionLabel:{default:'Retomar desde hoy'}});defineEmits(['action'])</script>
<template><section class="return"><p class="return__eyebrow">{{eyebrow}}</p><h2>{{headline}}</h2><p class="return__body">{{body}}</p><slot/><button v-if="actionLabel" type="button" @click="$emit('action')">{{actionLabel}}</button></section></template>
<style scoped>.return{display:flex;padding:var(--pad-card-lg);flex-direction:column;gap:10px;border:1px solid color-mix(in srgb,var(--accent-return) 32%,transparent);border-radius:var(--radius-xl);background:var(--aurora-return),var(--surface-primary);box-shadow:var(--elev-2)}.return__eyebrow,.return__body{margin:0}.return__eyebrow{color:var(--accent-return);font:600 12px/1 var(--font-core);letter-spacing:.06em;text-transform:uppercase}.return h2{margin:0;color:var(--text-primary);font:400 30px/1.18 var(--font-editorial);letter-spacing:-.01em}.return__body{color:var(--text-secondary);font:400 15px/1.55 var(--font-core);text-wrap:pretty}.return button{align-self:flex-start;min-height:48px;margin-top:6px;padding:0 22px;border:0;border-radius:var(--radius-pill);background:var(--action-primary);color:var(--action-primary-fg);font:600 15px/1 var(--font-core);cursor:pointer;transition:background var(--dur-fast) var(--ease-calm),transform var(--dur-instant) var(--ease-calm)}.return button:hover{background:var(--action-primary-hover)}.return button:active{transform:scale(var(--press-scale))}</style>
